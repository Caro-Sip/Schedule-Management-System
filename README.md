# Schedule-Management-System
Object Oriented Programming Proposed Project

Project Structure:

- Run DatabaseInit.java to initalize database(make the .db file)
- DAO directory is where the crud is being perform
- Config is where we config the database (links, connection...)
- Objects is where we create class
- Service is .java that verify user input
- Javascript also verify user input but for security reason we have another one in java

